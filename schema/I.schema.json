{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "I — Intention Layer",
  "description": "What the agent is doing right now: the goal, the user's purpose, and the success criteria. Rewritten per session.",
  "type": "object",
  "required": ["goal", "user_purpose", "success_criteria"],
  "additionalProperties": false,
  "properties": {
    "goal": {
      "type": "string",
      "description": "The primary objective the agent is pursuing right now."
    },
    "user_purpose": {
      "type": "string",
      "description": "What the user actually wants — interpreted, not just literal."
    },
    "success_criteria": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Observable conditions that indicate the goal has been met."
    }
  }
}
