{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "C — Context Layer (static)",
  "description": "Static state: how the agent IS. Persona, voice tone, temperature. The stable home position, independent of any current task. Context answers 'where/who am I?' — never 'where am I going?'.",
  "type": "object",
  "required": ["persona", "voice_tone", "temperature"],
  "additionalProperties": false,
  "properties": {
    "persona": {
      "type": "string",
      "description": "One-sentence description of the agent's character."
    },
    "voice_tone": {
      "type": "string",
      "description": "Communication style: formal, terse, warm, sarcastic, methodical, etc."
    },
    "temperature": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Creativity vs determinism scale. 0.0 = strict, 1.0 = imaginative."
    }
  }
}
