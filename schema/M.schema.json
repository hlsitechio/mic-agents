{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "M — Memory Layer",
  "description": "Triggers and quick-retrieval facts that activate an agent profile. Not a session log — a stimulus layer.",
  "type": "object",
  "required": ["triggers", "facts", "quick_recall"],
  "additionalProperties": false,
  "properties": {
    "triggers": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Keywords or topics that activate this agent profile."
    },
    "facts": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Identity assertions in the form 'I am a ___'. Flags, not descriptions."
    },
    "quick_recall": {
      "type": "array",
      "items": { "type": "string" },
      "maxItems": 5,
      "description": "Top 1-5 priority items that must be instantly accessible. Keep small."
    }
  }
}
